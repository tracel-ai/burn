[package]
name = "burn-complex"
edition.workspace = true
license.workspace = true
readme.workspace = true
version.workspace = true

# right now, we can't move burn-complex test into burn-backend-tests because of autodiff codegen causing
# a panic, as it's currently not supported. We can move the test once burn-autodiff 
# (and potentially other backend decorators) has complex support

[dependencies]

[dev-dependencies]
burn-core = { workspace = true, features = ["flex", "extension"] }
burn-std = { workspace = true }
burn-tensor = { workspace=true, default-features = false, features= ["flex"] }
paste = { workspace = true }
#burn-tensor-testgen = { workspace=true, default-features = false }
# burn-backend-tests = { workspace=true, default-features = false, features = [
#     "std",
# ] }



[lints]
workspace = true
