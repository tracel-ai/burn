[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science", "no-std", "embedded", "wasm"]
description = "Flexible and Comprehensive Deep Learning Framework in Rust"
documentation = "https://docs.rs/burn"
edition.workspace = true
keywords = [
    "deep-learning",
    "machine-learning",
    "tensor",
    "pytorch",
    "autodiff",
]
license.workspace = true
name = "burn"
readme.workspace = true
repository = "https://github.com/tracel-ai/burn"
rust-version = "1.95"
version.workspace = true

[lints]
workspace = true

[features]
default = [
    "rl",
    "std",
    "optim",
    "burn-core/default",
    "burn-train?/default",
    "burn-nn/default",
    "burn-linalg?/default",
    "burn-optim?/default",
    "burn-signal?/default",
    "burn-vision?/default",
]
doc = [
    "default",
    "train",
    "burn-core/doc",
    "burn-train/doc",
    "burn-store?/std",
    "burn-nn/doc",
    "linalg",
    "burn-optim?/doc",
    "signal",
]
std = [
    "burn-core/std",
    "burn-store?/std",
    "burn-nn/std",
    "burn-linalg?/std",
    "burn-signal?/std",
    "burn-optim?/std",
]
tracing = [
    "cubecl?/tracing",
    "burn-core/tracing",
    "burn-signal?/tracing",
    "burn-vision?/tracing",
    "burn-nn/tracing",
    "burn-linalg?/tracing",
    "burn-optim?/tracing",
]


network = ["burn-core/network"]

# Training with full features
train = ["burn-train", "optim", "dataset"]

## Includes the Text UI (progress bars, metric plots)
tui = ["burn-train?/tui"]

##  Includes system info metrics (CPU/GPU usage, etc)
metrics = ["burn-train?/sys-metrics"]

# Datasets
dataset = ["burn-core/dataset"]

sqlite = ["burn-core/sqlite"]
sqlite-bundled = ["burn-core/sqlite-bundled"]

# Model storage and serialization (SafeTensors, PyTorch interop)
store = ["burn-store"]

# Enable safetensors in burn-store.
safetensors = ["store", "burn-store?/safetensors"]
# Enable pytorch in burn-store.
pytorch = ["store", "burn-store?/pytorch"]

# CubeCL re-export
cubecl = ["dep:cubecl"]

audio = ["burn-core/audio"]
vision = ["burn-core/vision", "burn-vision"]
linalg = ["dep:burn-linalg"]
signal = ["dep:burn-signal"]
rl = ["dep:burn-rl", "burn-train?/rl"]

# Optimizer
optim = ["burn-optim", "autodiff"]

# Backend
ir = ["burn-core/ir"]
autodiff = ["burn-core/autodiff", "burn-signal?/autodiff"]
fusion = ["ir", "burn-core/fusion", "burn-linalg?/fusion", "burn-signal?/fusion", "burn-vision?/fusion"]

## Backend features
accelerate = ["burn-core/accelerate"]
autotune = ["burn-core/autotune", "burn-signal?/autotune"]
autotune-checks = ["burn-core/autotune-checks"]
blas-netlib = ["burn-core/blas-netlib"]
openblas = ["burn-core/openblas"]
openblas-system = ["burn-core/openblas-system"]
simd = ["burn-core/simd"]
rayon = ["burn-core/rayon"]

# Opt-in CPU codegen paths.
x86-v4 = ["burn-core/x86-v4"]
apple-amx = ["burn-core/apple-amx"]

template = ["burn-core/template"]
remote = ["burn-core/remote", "burn-linalg?/remote", "burn-signal?/remote"]
remote-server = ["burn-core/remote-server", "burn-signal?/router"]
remote-websocket = ["burn-core/remote-websocket", "burn-signal?/remote-websocket"]

cuda = ["burn-core/cuda", "burn-linalg?/cuda", "burn-signal?/cuda", "burn-vision?/cuda"]
rocm = ["burn-core/rocm", "burn-linalg?/rocm", "burn-signal?/rocm", "burn-vision?/rocm"]
flex = ["burn-core/flex", "burn-linalg?/flex", "burn-signal?/flex", "burn-vision?/flex"]
ndarray = ["burn-core/ndarray", "burn-linalg?/ndarray"]
tch = ["burn-core/tch", "burn-linalg?/tch", "burn-signal?/tch", "burn-vision?/tch"]
vulkan = ["wgpu", "burn-core/vulkan", "burn-linalg?/vulkan", "burn-signal?/vulkan", "burn-vision?/vulkan"]
webgpu = ["wgpu", "burn-core/webgpu", "burn-linalg?/webgpu", "burn-signal?/webgpu", "burn-vision?/webgpu"]
metal = ["wgpu", "burn-core/metal", "burn-linalg?/metal", "burn-signal?/metal", "burn-vision?/metal"]
wgpu = ["burn-core/wgpu", "burn-linalg?/wgpu", "burn-signal?/wgpu", "burn-vision?/wgpu"]
cpu = ["burn-core/cpu", "burn-linalg?/cpu", "burn-signal?/cpu", "burn-vision?/cpu"]

# Backend extensions
extension = ["burn-core/extension"]
capture = ["burn-core/capture", "burn-linalg?/capture", "burn-signal?/capture"]

[dependencies]

# ** Please make sure all dependencies support no_std when std is disabled **

burn-core = { workspace = true }
burn-linalg = { workspace = true, optional = true }
burn-train = { workspace = true, optional = true }
burn-store = { workspace = true, optional = true }
burn-nn = { workspace = true }
burn-optim = { workspace = true, optional = true }
burn-rl = { workspace = true, optional = true }
burn-signal = { workspace = true, optional = true }
burn-vision = { workspace = true, optional = true }
burn-std = { workspace = true }

# Backend implementations and their public facade re-exports are owned by burn-core;
# dispatch itself continues through burn-tensor -> burn-dispatch.

cubecl = { workspace = true, optional = true }
