[package]
name = "burn-import-model-checks-albert"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[features]
default = ["tch"]
ndarray = []
tch = []
wgpu = []
metal = []

[dependencies]
burn = { path = "../../../../crates/burn", features = [
    "ndarray",
    "tch",
    "wgpu",
    "metal",
] }
burn-store = { path = "../../../../crates/burn-store", features = ["burnpack", "pytorch"] }

[build-dependencies]
burn-import = { path = "../../../burn-import" }
