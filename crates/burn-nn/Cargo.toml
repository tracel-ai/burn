[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science", "no-std", "embedded", "wasm"]
description = "Neural network building blocks for the Burn deep learning framework"
documentation = "https://docs.rs/burn-nn"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "tensor", "pytorch", "ndarray"]
license.workspace = true
name = "burn-nn"
readme.workspace = true
repository = "https://github.com/tracel-ai/burn/tree/main/crates/burn-nn"
version.workspace = true

[lints]
workspace = true

[features]
default = ["std", "burn-core/default"]
doc = [
    "std",
    # Doc features
    "burn-core/doc",
]
std = ["burn-core/std", "num-traits/std"]

tracing = ["burn-core/tracing"]

# Test backends
test-cuda = ["burn-core/cuda"]
test-rocm = ["burn-core/rocm"]
test-ndarray = ["burn-core/ndarray"]
test-tch = ["burn-core/tch"]
test-vulkan = ["burn-core/vulkan"]
test-wgpu = ["burn-core/wgpu"]
test-metal = ["burn-core/metal"]

# Memory checks are disabled by default
test-memory-checks = ["burn-core/test-memory-checks"]

[dependencies]

# ** Please make sure all dependencies support no_std when std is disabled **
burn-core = { path = "../burn-core", version = "=0.21.0-pre.3", default-features = false }

num-traits = { workspace = true }

[dev-dependencies]
burn-ndarray = { path = "../burn-ndarray", version = "=0.21.0-pre.3" }
burn-autodiff = { path = "../burn-autodiff", version = "=0.21.0-pre.3" }
burn-core = { path = "../burn-core", version = "=0.21.0-pre.3", default-features = false, features = [
    "autodiff",
] }
rstest = { workspace = true }

[package.metadata.docs.rs]
features = ["doc"]
rustdoc-args = ["--cfg", "docsrs"]
