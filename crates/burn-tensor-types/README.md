# Burn Tensor Types

Core type definitions for Burn's tensor stack: `Shape`, `DType`, `TensorData`, `Slice`,
`Distribution` types, and related utilities. Used by backend traits and tensor abstractions.
