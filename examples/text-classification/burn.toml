[fusion]
logger = { file = "/tmp/fusion.log", level = "disabled" }
beam_search = { max_blocks = 1, max_explorations = 100 }

[remote]
logger = { file = "/tmp/remote.log", level = "full" }

[cubecl.profiling]
logger = { log = "info", level = "disabled" }

[cubecl.autotune]
level = "balanced"
cache = "target"
logger = { info = true, level = "disabled" }

[cubecl.compilation]
logger = { level = "disabled" }
cache = "target"
check_mode = "auto"

[cubecl.memory]
logger = { level = "disabled", file = "/tmp/memory.log" }
persistent_memory = "enabled"

[cubecl.streaming]
max_streams = 8
